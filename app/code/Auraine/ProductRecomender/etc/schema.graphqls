type Query {
    getFrequentProducts
    (
        id: Int @doc(description: "Id of the product")

    )
    : [String] @resolver( class: "Auraine\\ProductRecomender\\Model\\Resolver\\ProductsList") @doc(description: "List of Frequent Products") @cache(cacheIdentity: "Auraine\\ProductRecomender\\Model\\Resolver\\ProductsList\\Identity")
}
